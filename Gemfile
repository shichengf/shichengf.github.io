source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Optional on Windows (`gem install wdm`) after MSYS2 via `ridk install`; omit here so Ruby 3.4+ builds without native toolchain.

group :jekyll_plugins do
  gem "jekyll-feed"
  gem 'jekyll-sitemap'
  gem 'hawkins'
  gem "webrick", "~> 1.8"
end
